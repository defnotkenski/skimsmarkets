# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
build/
dist/

# Virtual environments
.venv/
venv/

# Environment / secrets
.env
.env.*
!.env.example

# Tool caches
.ruff_cache/
.mypy_cache/
.pytest_cache/
.coverage
htmlcov/

# IDEs
.idea/
.vscode/

# Claude Code — ignore machine-specific contents (settings.local.json,
# worktrees/) but track shared skills so they sync alongside the playbooks.
.claude/*
!.claude/skills/

# OS
.DS_Store

# Backtest data cache (gamma events, CLOB price history, parquet)
backtest_cache/
BACKTEST_REPORT.md

# Per-run JSONL logs (director predictions + entry decisions)
logs/
